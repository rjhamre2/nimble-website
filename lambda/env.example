# Facebook App Configuration
FACEBOOK_APP_ID=your_facebook_app_id_here
FACEBOOK_APP_SECRET=your_facebook_app_secret_here

# Frontend URL (optional - CORS is configured to allow multiple origins)
FRONTEND_URL=https://nimbleai.in

# Environment
NODE_ENV=production

# Firebase Configuration (via AWS Secrets Manager)
FIREBASE_SECRET_NAME=firebase-key-unUSpd

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Facebook App credentials
# 3. Set these same values in your AWS Lambda environment variables
# 4. For Firebase, store your service account key in AWS Secrets Manager
#    and set FIREBASE_SECRET_NAME to the secret name 